{
	"name": "pilotjs-project",
	"version": "2.0.0-beta.3",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"engines": {
		"node": ">=14.0.0"
	},
	"scripts": {
		"lerna": "lerna",
		"publish-packages": "turbo run build && changeset version && changeset publish",
		"publish-packages-canary": "turbo run build && changeset version --snapshot canary --tag canary && changeset publish",
		"publish-packages-next": "turbo run build && changeset version --snapshot next --tag next && changeset publish"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/Wave-Play/pilot.git"
	},
	"publishConfig": {
		"access": "public",
		"registry": "https://registry.npmjs.org/"
	},
	"license": "MIT",
	"author": "WavePlay <dev@waveplay.com> (waveplay.com)",
	"contributors": [
		"Pkmmte Xeleon <pk@waveplay.com>"
	],
	"devDependencies": {
		"conventional-changelog-cli": "^2.2.2",
		"lerna": "^6.0.1",
		"turbo": "1.6.3"
	},
	"dependencies": {
		"@changesets/cli": "2.25.2"
	}
}
